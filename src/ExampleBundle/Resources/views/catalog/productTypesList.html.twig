<div class="row">
    <h3>Types:</h3>
    <ul class="list-inline categories-menu">
        {% for productType in productTypes %}
            <li class="category-item"><a href="{{ path('_ctp_example_products_of_productType', {productTypeId: productType.id}) }}">{{ productType.name }}</a></li>
        {% endfor %}
    </ul>
</div>
