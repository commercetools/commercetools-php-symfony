<div class="checkout-summary">
    <div class="text-uppercase">
        <span class="checkout-summary-title">{{ 'shippingMethod'|trans([], 'checkout') }}</span>
        {% include '@Example/partials/checkout/edit-link.html.twig' with {'id':"confirmation-edit-method-btn", 'url':meta._links.editShippingMethod.href, 'enabled':editable} %}
    </div>
    <div class="checkout-summary-text">
        <span>{{ shippingInfo.shippingMethodName }}</span>
        <br>
        <span>{{ shippingInfo.description }}</span>
    </div>
</div>
