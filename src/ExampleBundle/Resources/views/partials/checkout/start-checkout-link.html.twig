<div class="checkout-now {% if bottom %}checkout-now-bottom{% endif %}">
  <a href="{{ path('_ctp_example_checkout_address') }}" class="pull-right text-uppercase checkout-now-btn" {% if id %}id="{{ id }}"{% endif %}>{{ 'common.checkout'|trans([], 'main') }}</a>
</div>
