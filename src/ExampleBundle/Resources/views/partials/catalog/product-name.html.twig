<p>
  <h1 class="text-uppercase pdp-product-title">{{ product.name }}</h1>
  {% if wishlist %}
    <span class="grey-p quickview-sku">{{ product.variant.sku }}</span>
  {% else %}
    <span class="grey-p quickview-sku">{{ product.masterVariant.sku }}</span>
  {% endif %}
</p>
