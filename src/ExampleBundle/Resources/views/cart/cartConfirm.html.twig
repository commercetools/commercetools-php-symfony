{% extends "ExampleBundle::layout.html.twig" %}

{% block container %}

    {% include 'ExampleBundle:partials:address.html.twig' with {address: cart.getShippingAddress} %}

    {% if cart.getBillingAddress %}
        {% include 'ExampleBundle:partials:address.html.twig' with {address: cart.getBillingAddress} %}
    {% else %}
        {% include 'ExampleBundle:partials:address.html.twig' with {address: cart.getShippingAddress} %}
    {% endif %}

    {% include 'ExampleBundle:partials:shippingProvider.html.twig' with {shippingInfo: cart.getShippingInfo} %}

    {% include 'ExampleBundle:partials:cartView.html.twig' with {cart: cart} %}

{% endblock %}