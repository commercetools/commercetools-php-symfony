{% block head %}
<!DOCTYPE html>
    <html>
        <head>
            <title>{% block title %}  Awesome webapp by Commercetools {% endblock %}</title>

            {% block stylesheets %}
                <link rel="stylesheet" href="{{ asset('bundles/ctp/css/bootstrap.css') }}" />
                <link rel="stylesheet" href="{{ asset('bundles/ctp/css/style.css') }}" />
            {% endblock %}

            {% block javascripts %}
                <script src="{{ asset('bundles/ctp/js/jquiry-2.3.3.js') }}"></script>
                <script src="{{ asset('bundles/ctp/js/index.js') }}"></script>
            {% endblock %}
        </head>
{% endblock %}

{% block menu %}
    <div class="menu">
       <a href="{{ path('_ctp_example') }}">
            <img class="menu-logo" src="{{ asset('bundles/ctp/img/CT-logo.svg') }}" alt="example"/>
       </a>
        <ul>
            <li><a href="{{ path('_ctp_example') }}">Home</a> </li>
            <li><a href="{{ path('_ctp_example_cart') }}">Cart</a> </li>
            {% if app.user %}
                <li><a href="{{ path('_ctp_example_user_details') }}">User</a> </li>
                <li><a href="{{ path('_ctp_example_user_address_book') }}">Adressen</a> </li>
                <li><a href="{{ path('_ctp_example_orders_all') }}">Orders</a> </li>
                <li><a href="{{ path('logout') }}">logout</a> </li>
            {% endif %}
            {% if not app.user %}
                <li><a href="{{ path('login') }}">login</a> </li>
            {% endif %}
        </ul>
    </div>

{% endblock %}

{% block body %}
    <body>
        <div class="content">
            {% block container %}
            {% endblock %}
        </div>
    </body>
{% endblock %}


